name: Third Workflow
on: push

jobs:
    run-first-command:
        runs-on: ubuntu-latest
        steps: 
            - name: Command in multiple lines
              run: |
                pwd
                ls -a